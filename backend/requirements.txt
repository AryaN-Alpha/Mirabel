# --- Web framework ---
Django==6.0.4
djangorestframework==3.16.0
django-cors-headers==4.7.0

# --- Database ---
mysqlclient==2.2.7
# If mysqlclient compilation gives you trouble on macOS/Windows,
# swap it for: PyMySQL==1.1.1  (and add a small shim in mirabel/__init__.py)

# --- Config / env ---
python-dotenv==1.0.1
pydantic==2.10.4
pydantic-settings==2.7.1

# --- AI core ---
anthropic==0.42.0

# --- HTTP / utilities ---
requests==2.32.3
httpx==0.28.1
tenacity==9.0.0

# --- Production server ---
gunicorn==23.0.0
whitenoise==6.8.2

# --- Dev / testing ---
pytest==8.3.4
pytest-django==4.9.0
ruff==0.9.1

# --- Phase 2: memory + async ---
celery==5.5.0
redis==5.2.1
# chromadb-client: lightweight HTTP client only (no C++ build needed on Windows).
# Full chromadb can be used on Linux/macOS by replacing this with chromadb==0.6.3+.
chromadb-client==1.5.8
django-celery-beat==2.7.0
django-celery-results==2.5.1
